---
export const prerender = true;
import SchoolsPage from '@/components/SchoolsPage.astro';
import { getLocale } from '@/i18n';

const locale = getLocale(Astro.url);
---
<SchoolsPage locale={locale} />