---
export const prerender = true;
import EducatorsPage from '@/components/EducatorsPage.astro';
import { getLocale } from '@/i18n';

const locale = getLocale(Astro.url);
---

<EducatorsPage locale={locale} />
