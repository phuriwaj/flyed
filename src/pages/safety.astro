---
export const prerender = true;
import SafetyPage from '@/components/SafetyPage.astro';
import { getLocale } from '@/i18n';

const locale = getLocale(Astro.url);
---

<SafetyPage locale={locale} />
