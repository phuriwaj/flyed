---
export const prerender = true;
import ParentsPage from '@/components/ParentsPage.astro';
import { getLocale } from '@/i18n';

const locale = getLocale(Astro.url);
---
<ParentsPage locale={locale} />