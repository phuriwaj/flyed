---
export const prerender = true;
import EnquirePage from '@/components/EnquirePage.astro';
import { getLocale } from '@/i18n';

const locale = getLocale(Astro.url);
---

<EnquirePage locale={locale} />
