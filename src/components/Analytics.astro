---
import { PUBLIC_ANALYTICS_HOST } from 'astro:env/client';

const host = PUBLIC_ANALYTICS_HOST;
if (!host) {
  // No analytics configured — render nothing
}
---

{host && <script type="text/partytown" src={`${host}/script.js`} data-domain="flyed.dev" defer />}
