# Build output
dist/
.astro/

# Dependencies
node_modules/

# Lockfile — never reformat
package-lock.json

# Binary assets
*.png
*.avif
*.webp
*.ico
*.gif

# Wrangler config (JSONC with comments — Prettier will mangle these)
wrangler.jsonc

# Cloudflare custom config (not JSON — special formats)
public/_headers
public/_redirects

# Test artifacts
playwright-report/
test-results/
coverage/
.lighthouseci/

# Plan/spec markdown lives outside the product; leave the diff for humans
.superpowers/
docs/superpowers/

# Agent worktrees (clone of the repo for subagent isolation) — never
# format or lint inside these; they're separate workspaces.
.claude/worktrees/

# Stray Chrome profile files that were accidentally committed with
# absolute Windows-style paths. Remove via `git rm 'undefined:*'` once
# a clean rebase is possible; for now ignore them so format:check passes.
undefined:*

# Design doc is hand-edited prose with custom formatting; prettier's
# default breaks the visual structure the author chose.
DESIGN.md

# SDD task reports — generated by the harness, may include code fences with
# unparseable file contents (e.g. one report shells out Astro code that the
# prettier-plugin-astro parser rejects mid-file)
.task-reports/
